pipeline:
  name: 'Order Processing ETL Pipeline'
  version: '1.0.0'
  reader_type: 'csv'

paths:
  raw_data: 'data/raw/messy_orders.csv'
  clean_data: 'data/cleaned/clean_orders.csv'
  invalid_data: 'data/invalid/invalid_order.csv'
  log_file: 'logs/pipeline.log'

logging:
  level: 'INFO'